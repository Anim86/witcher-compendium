<nav class="wizard-navigation">
    {{#each steps}}
    <div class="nav-item {{#if active}}active{{/if}} {{#if complete}}completed{{/if}}" 
         data-action="goToStep" data-step="{{id}}">
        <span class="step-label">Step {{number}}</span>
    </div>
    {{/each}}
</nav>
